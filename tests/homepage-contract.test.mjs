import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync,existsSync} from 'node:fs';
const page=readFileSync('app/page.tsx','utf8'),css=readFileSync('app/globals.css','utf8');
test('real approved logo and honest illustrative demonstration',()=>{assert.ok(existsSync('public/brand/rextexh-logo.png'));assert.match(page,/Illustrative example/);assert.doesNotMatch(page,/RexTexh OS|crown-glyph|mailto:/);});
test('concise headline and functioning mobile navigation controls',()=>{assert.match(page,/Better work\./);assert.match(page,/Built with AI/);assert.match(page,/aria-expanded=\{open\}/);assert.match(page,/aria-controls="navigation"/);assert.match(page,/id="contact"/);assert.match(page,/id="services"/);});
test('motion decay and reduced-motion lifecycle cleanup exist',()=>{assert.match(page,/setTimeout\(reset,1300\)/);assert.match(page,/onPointerLeave=\{reset\}/);assert.match(page,/removeEventListener/);assert.match(page,/clearTimeout/);assert.match(css,/prefers-reduced-motion/);});
